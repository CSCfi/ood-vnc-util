[Desktop Entry]
Type=Application
Name=Visit
Terminal=true
Exec=bash -c 'ssh -t $HOSTNAME "export DISPLAY=$DISPLAY;export TMPDIR=$TMPDIR;export XDG_RUNTIME_DIR=$XDG_RUNTIME_DIR;module load visit;visit"'
Icon=<%= File.join(session.staged_root, "icons", "visit.jpg") -%>
